@{
  Layout = "_Layout";
}

<h1>Welcome to the PDX Band Index!</h1>
<div class="row">
  <div class="col-md-3">
    <p>@Html.ActionLink("See all bands", "Index", "Bands")</p> <p>@Html.ActionLink("See all genres", "Index", "Genres")</p>
    <p>@Html.ActionLink("See all shows", "Index", "Shows")</p>
    @if (User.Identity.IsAuthenticated) {
      <p>@Html.ActionLink("Account Page", "Index", "Account")</p>
    }
    else
    {
    <p>@Html.ActionLink("Create an account", "Index", "Account")</p>
    }
  </div>
  <div class="col-md-4">
    <h1>Test</h1>
  </div>
  <div class="col-md-4">
    <h1>Test</h1>
  </div>
</div>